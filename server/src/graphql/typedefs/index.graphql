scalar Date

type Query {
    users: [Users]!
}

type Mutation {
    createAccount(user: UserCreateInput!): Signup!
    loginAccount(user: UserLoginInput!): Token!
    confirmationCode(code: Int!): Token!
}
